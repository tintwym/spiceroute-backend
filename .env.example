# Database — choose ONE.
# Local Docker Postgres (requires `docker compose --profile local-db up`):
DATABASE_URL=postgresql+asyncpg://spiceroute:spiceroute@db:5432/spiceroute
# Neon (managed Postgres). Use the `?ssl=require` query param (asyncpg, not libpq):
# DATABASE_URL=postgresql+asyncpg://USER:PASS@ep-xxx.REGION.aws.neon.tech/DBNAME?ssl=require

# LLM provider — OpenAI-compatible Chat Completions API (AI Creator / chat).
# Leave blank for stub mode. Curated translations use GEMINI_API_KEY below.
#
# Optional — OpenAI:
#   LLM_BASE_URL=https://api.openai.com/v1
#   LLM_MODEL=gpt-4o-mini
#   LLM_API_KEY=sk-...
#
# Optional — local Ollama:
#   LLM_BASE_URL=http://localhost:11434/v1
#   LLM_MODEL=llama3.1:8b
#   LLM_API_KEY=ollama
#
LLM_BASE_URL=
LLM_MODEL=
LLM_API_KEY=

# Gemini — curated recipe translation (scripts/translate_curated_recipes.py).
# Free tier: https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# When LLM_BASE_URL or LLM_API_KEY is empty (or AI_FORCE_STUB=1) the
# AI endpoints serve deterministic stub content per request — useful
# in CI, offline dev, and during initial Render setup before a key
# has been pasted.
AI_FORCE_STUB=

# AI rate limits per client IP
AI_RATE_LIMIT_PER_DAY=30
AI_CHAT_PER_HOUR=50

# Firebase Admin — TWO ways to provide credentials, pick one:
#
#  (a) Path to a service-account JSON on disk (best locally — drop the
#      file at the repo root and forget about it).
FIREBASE_CREDENTIALS_PATH=firebase-service-account.json
#
#  (b) The same JSON inlined as a single string (best on hosted
#      platforms — Fly.io, Render, Railway, where you set this via
#      `fly secrets set FIREBASE_CREDENTIALS_JSON="$(cat …)"` or the
#      dashboard's env editor). Takes precedence over (a) when set.
# FIREBASE_CREDENTIALS_JSON=
#
# When NEITHER resolves, the backend runs in DEV MODE and accepts
# tokens of the form "dev:<uid>" — useful for tests and offline dev.

# CORS — two complementary knobs.
#
# CORS_ORIGINS — comma-separated EXACT origins. No wildcards (string
#                match only). Use this for production with fixed
#                domains. "*" disables CORS protection — local dev only.
#
# CORS_ORIGIN_REGEX — Python regex matched against the request's Origin
#                     header. The only way to express wildcards. Common
#                     pattern is allowing every Vercel preview deploy:
#                       CORS_ORIGIN_REGEX=^https://([a-z0-9-]+\.)*vercel\.app$
#
# Both can be set together — middleware accepts a request whose Origin
# matches EITHER. Leave CORS_ORIGIN_REGEX blank if you don't need it.
#
# Examples:
#   Local dev:        CORS_ORIGINS=*
#   Single domain:    CORS_ORIGINS=https://spiceroute-recipe.vercel.app
#   Domain + previews:
#     CORS_ORIGINS=https://spiceroute-recipe.vercel.app
#     CORS_ORIGIN_REGEX=^https://([a-z0-9-]+\.)*vercel\.app$
CORS_ORIGINS=*
CORS_ORIGIN_REGEX=

# App
APP_NAME=SpiceRoute API
DEBUG=true